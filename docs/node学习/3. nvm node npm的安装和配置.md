# 3. nvm node npm的安装和配置

```bash
npm i -D eslint

# 当前文件夹初始化
# 1. windows需要上下键选择，写第二行 2.并以管理员运行
`./node_modules/.bin/eslint --init` 
`winpty ./node_modules/.bin/eslint.cmd --init`

# 安装风格，我用npx失败，换成npm install可以
npx install-peerdeps --dev eslint-config-airbnb-base

# 编辑器eslint插件打开
```

